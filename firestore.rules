rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Cho phép đọc và ghi cho tất cả người dùng (CHỈ DÙNG CHO PHÁT TRIỂN)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}